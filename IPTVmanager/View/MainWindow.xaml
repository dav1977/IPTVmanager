<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:IPTVman.Helpers"
        xmlns:vm="clr-namespace:IPTVman.ViewModel" 
        x:Class="IPTVman.ViewModel.MainWindow" WindowStartupLocation="CenterScreen"
        Title="MainWindow" Height="595.625" Width="998.708" Background="#FF282076" 
        DataContext="{DynamicResource ViewModelMain}" Foreground="#FF20D10F">
    <Window.Resources>
        <vm:ViewModelMain x:Key="ViewModelMain"/>
    </Window.Resources>
    <Grid Margin="0,0,65,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="28*"/>
            <ColumnDefinition Width="115*"/>
            <ColumnDefinition Width="784*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.939*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <GroupBox Header="Classic INotifyPropertyChanged Example" HorizontalAlignment="Center" VerticalAlignment="Center" BorderBrush="#FFD3E04B" Foreground="#FF26F55F" Margin="5,44,130,10" Width="763" Height="490" Grid.ColumnSpan="2" Grid.Column="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="139*" />
                    <RowDefinition Height="85*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <ScrollViewer VerticalScrollBarVisibility="Auto" Grid.RowSpan="2" Margin="10,0,0,101">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <ListBox x:Name="box1" ItemsSource="{Binding People}" SelectedItem="{Binding SelectedPerson}" DisplayMemberPath="FirstName" HorizontalAlignment="Left" SelectionChanged="ListBox_SelectionChanged"/>
                            <DataGrid x:Name="grid1" ItemsSource="{Binding People}" SelectedItem="{Binding SelectedPerson}" HorizontalAlignment="Left" Margin="5,0,0,0" Width="314" SelectionChanged="DataGrid_SelectionChanged"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
                <Button Content="УДАЛИТЬ канал" Command="{Binding key_delCommand}" CommandParameter="{Binding Text, ElementName=tb1}" Margin="578,136.183,10,10" Focusable="False" Grid.Row="1"/>
                <Button Content="Добавить канал" Command="{Binding key_ADDCommand}" CommandParameter="{Binding Text, ElementName=tb1}" Margin="578,106.183,10,40" Focusable="False" Grid.Row="1"/>
                <TextBox x:Name="tb1" Text="новый канал" Margin="265,0,0,40" TextChanged="tb1_TextChanged" Width="311" HorizontalAlignment="Left" VerticalAlignment="Bottom" Grid.Row="1"/>
                <Label x:Name="label" Content="Label" HorizontalAlignment="Left" Margin="84,160,0,0" Grid.Row="1" VerticalAlignment="Top"/>
            </Grid>
        </GroupBox>
        <Button Content="OpenFILE" Command="{Binding key_OPENCommand}" CommandParameter="{Binding Text, ElementName=tb1}" Margin="15,10,10,502" Focusable="False" Grid.Column="1"/>
        <Button Content="Next example" VerticalAlignment="Top" HorizontalAlignment="Right" FontWeight="Bold" Foreground="Red" Click="Button_Click" Grid.Row="1" Margin="0,0,36,0" Grid.Column="2"/>
        <ComboBox x:Name="combo1" ItemsSource="{Binding People}" SelectedItem="{Binding SelectedPerson}" DisplayMemberPath="FirstName" Margin="144,10,309,0" VerticalAlignment="Top" RenderTransformOrigin="6.068,0.97" Height="28" SelectionChanged="ComboBox_SelectionChanged" Grid.Column="2"/>
        <Label x:Name="label1" Content="Всего каналов:" Grid.Column="1" HorizontalAlignment="Left" Margin="49,478,0,0" VerticalAlignment="Top" FontSize="20" Foreground="#FF0FD63C" Grid.ColumnSpan="2"/>
        <Label x:Name="label_kanals" Content="0" Grid.Column="2" HorizontalAlignment="Left" Margin="86,478,0,0" VerticalAlignment="Top" FontSize="20" Foreground="#FF0FD63C" Width="226"/>

    </Grid>
</Window>
